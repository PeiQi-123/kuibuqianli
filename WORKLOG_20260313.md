# 2026-03-13 工作日志

## 今日目标

- 打通真实 AI 微运动推荐链路
- 完成健康数据页与运动记录闭环
- 约束 AI 推荐动作只能来自现有视频库
- 优化视频指导页，使视频展示与 AI 推荐动作保持一致

## 今日完成事项

### 1. 运行环境与问题排查

- 检查并整理本地前端、后端、AI 服务的重复进程。
- 重整运行环境：关闭重复实例后，仅保留一套前端 / 后端 / AI 服务。
- 编写资源检查脚本 `scripts/check_project_resources.ps1`，用于排查项目进程和内存占用。
- 分析并确认高内存 / 高 CPU 主要来自 Flutter Web 调试、Spring Boot、Python AI 服务和浏览器调试进程。

### 2. 数据库与后端基础修复

- 修复 `user` 表结构与当前代码不一致的问题。
- 新增数据库迁移脚本 `database/init/02_alter_user_table_add_profile_columns.sql`。
- 补齐用户健康相关字段：
  - `height`
  - `weight`
  - `bmi`
  - `bmi_type`
  - `age`
  - `gender`
  - `remind_enabled`
  - `remind_interval`
  - `remind_max_times`
  - `remind_avoid_time`
- 解决登录后查询用户信息时报 `Unknown column 'height'` 的问题。

### 3. 编码与响应处理修复

- 更新 `scripts/start_backend.bat`，统一后端启动编码为 UTF-8。
- 更新 `backend/src/main/java/com/kuibuqianli/config/JacksonConfig.java`，改善 JSON / 字符串响应编码一致性。
- 解决 Windows 控制台日志中文乱码问题。
- 统一将 AI 返回中的 `*` 替换为空格，减少前端展示时的格式噪音。

### 4. 真实 AI 接口接入

- 确认 `ai-service` 中原有 `/api/motion/generate` 为示例接口，不是真实模型。
- 发现并接入后端 DeepSeek 真实链路：
  - `POST /api/micro-motion/generate-prompt`
- 更新前端 `frontend/lib/screens/motion_recommendation_screen.dart`，使推荐页调用真实 AI 接口。
- 更新 `ai-service/app/services/motion_service.py`，让 AI 服务代理到后端真实 AI 接口。
- 更新 `ai-service/app/api/endpoints/motion_generator.py`，使 Swagger 页面测试时也走真实 AI。

### 5. AI 输出结构重构

- 判断原有 AI 输出为自由文本，按换行拆步骤不合理。
- 重构 AI 输出为结构化 JSON。
- 扩展 `backend/src/main/java/com/kuibuqianli/dto/PromptResponse.java`，新增字段：
  - `title`
  - `overview`
  - `actions`
  - `tip`
- 每个 `action` 包含：
  - `name`
  - `seconds`
  - `instruction`
  - `warning`
- 更新 `DeepSeekService`：
  - 提示词要求 DeepSeek 只返回 JSON
  - 后端解析 JSON
  - 自动回退到兜底结构，避免 AI 返回异常格式时页面崩溃
- 更新前端推荐页与视频页，改为消费结构化动作数组，而不是拆行文本。

### 6. 视频库命名与 AI 动作约束

- 将 `D:\teach\kuibuqianli\kuibuqianli\video` 目录下原始录屏文件统一重命名为微运动名称。
- 当前视频库统一为可理解的微运动动作名，如：
  - `颈部拉伸.mp4`
  - `肩部环绕.mp4`
  - `坐姿猫牛式伸展.mp4`
  - `坐姿骨盆前后倾.mp4`
  - `腰部侧弯拉伸.mp4`
  - `深呼吸放松.mp4`
- 更新 `DeepSeekService`：
  - 在提示词中注入视频动作库
  - 只允许 AI 从视频动作库中选择动作名称
  - 如果 AI 返回了不在视频库里的动作，后端会进行强制纠正
  - 如果视频库为空，直接返回错误，不再生成无法播放的视频方案

### 7. 视频指导页优化

- 更新 `frontend/lib/screens/video_player_screen.dart`。
- 现在“查看视频指导”页：
  - 只展示本次 AI 推荐动作命中的视频
  - 不再把整个视频库全部列出来
  - 视频顺序按照 AI 推荐动作顺序排列
  - 默认自动选中第一个匹配视频
  - 支持按顺序自动播放下一个匹配视频
  - 仍保留手动上一条 / 下一条切换能力

### 8. 健康数据功能完善

- 新增后端健康数据接口：`GET /api/user/health-data`
- 新增 DTO / Service：
  - `HealthDataDTO`
  - `HealthDataService`
  - `HealthDataServiceImpl`
- 健康数据页已接入：
  - 身高、体重、年龄、性别
  - BMI / BMI 类型
  - 累计运动次数
  - 完成次数
  - 累计时长
  - 最近运动记录

### 9. 运动记录闭环

- 新增运动记录保存接口：`POST /api/motion/record`
- 新增 DTO / Service：
  - `ExerciseRecordCreateDTO`
  - `ExerciseRecordService`
  - `ExerciseRecordServiceImpl`
- 视频指导页新增按钮：
  - `完成本次运动并写入健康数据`
- 点击后会把本次训练写入 `exercise_record`，健康数据页可立即看到统计变化。

## 今日关键文件

- `backend/src/main/java/com/kuibuqianli/service/DeepSeekService.java`
- `backend/src/main/java/com/kuibuqianli/dto/PromptResponse.java`
- `backend/src/main/java/com/kuibuqianli/controller/MotionController.java`
- `backend/src/main/java/com/kuibuqianli/controller/UserController.java`
- `backend/src/main/java/com/kuibuqianli/service/impl/UserServiceImpl.java`
- `frontend/lib/screens/motion_recommendation_screen.dart`
- `frontend/lib/screens/video_player_screen.dart`
- `frontend/lib/screens/health_data_screen.dart`
- `frontend/lib/app/routes/app_routes.dart`
- `database/init/02_alter_user_table_add_profile_columns.sql`
- `scripts/check_project_resources.ps1`

## 验证情况

- 后端编译通过：`mvn -q -DskipTests compile`
- 前端关键页面分析通过：
  - `motion_recommendation_screen.dart`
  - `video_player_screen.dart`
  - `health_data_screen.dart`
  - `app_routes.dart`
- 已验证真实 AI 接口可返回结构化动作。

## 当前结果

- 微运动主流程已形成闭环：
  - 登录 -> 完善资料 -> AI 推荐 -> 视频指导 -> 完成打卡 -> 健康数据统计
- AI 推荐动作已与视频库强绑定。
- 视频指导页只展示本次推荐动作对应的视频，并支持顺序播放。
