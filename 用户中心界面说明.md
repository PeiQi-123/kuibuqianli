# 用户中心界面实现说明

## 已实现的功能

### 1. 应用主界面布局重构
- 将登录注册后进入的`AppScreen`改为带底部导航栏的布局
- 底部导航栏包含两个选项卡：**功能首页**和**用户中心**
- 功能首页保持原有的4个功能卡片（微运动推荐、视频指导、姿态检测、健康数据）
- 用户中心显示新的用户信息界面

### 2. 用户中心界面结构
用户中心界面包含以下部分：

#### 顶部用户信息卡片
- 用户头像（默认图标）
- 用户名显示
- 邮箱显示
- 运动统计数据（运动次数、累计时长、完成度）

#### 两个内部选项卡
1. **用户信息选项卡**
   - 基本信息（用户名、邮箱、注册时间、用户ID）
   - 健康信息（年龄、身高、体重、BMI）
   - 运动统计（本周运动次数、本月累计时长、平均每次时长、最喜欢运动）
   - 编辑个人信息按钮

2. **运动偏好选项卡**
   - 视频偏好设置（清晰度、播放速度、自动播放）
   - 运动类型偏好（拉伸类、力量类、有氧类、平衡类）
   - 提醒设置（运动提醒、提醒时间、提醒频率）
   - 保存偏好设置按钮

### 3. 技术实现细节

#### 文件结构
```
frontend/lib/screens/
├── home_screen.dart          # 登录前的主页（未修改）
├── app_screen.dart           # 修改后的应用主界面，包含底部导航栏
├── user_center_screen.dart   # 新增的用户中心界面
└── app_routes.dart           # 更新路由配置
```

#### 主要组件
- `AppScreen`: 使用`BottomNavigationBar`实现选项卡切换（登录后进入的界面）
- `UserCenterScreen`: 使用`TabBar`和`TabBarView`实现内部选项卡
- 响应式设计，适配不同屏幕尺寸

#### 状态管理
- 使用`StatefulWidget`管理选项卡状态
- 通过`AuthService`获取当前用户信息
- 选项卡状态使用`TabController`管理

### 4. 界面特点
- **现代化设计**: 使用Material Design组件
- **响应式布局**: 适配手机和平板
- **直观交互**: 清晰的选项卡和按钮
- **数据展示**: 展示用户信息和运动统计
- **偏好设置**: 提供丰富的个性化选项

### 5. 使用方式
1. 用户登录/注册后，进入应用主界面（AppScreen）
2. 在应用主界面可以看到底部导航栏
3. 点击"用户中心"选项卡进入用户信息界面
4. 在用户中心可以查看个人信息和设置运动偏好
5. 点击"首页"选项卡返回功能卡片页面

### 6. 扩展建议
- 添加头像上传功能
- 实现个人信息编辑功能
- 连接后端API保存用户偏好
- 添加更多的运动统计数据
- 实现深色模式支持

## 代码位置
- 用户中心界面: `frontend/lib/screens/user_center_screen.dart`
- 修改后的应用主界面: `frontend/lib/screens/app_screen.dart`
- 路由配置: `frontend/lib/app/routes/app_routes.dart`